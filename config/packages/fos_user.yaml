fos_user:
    db_driver: orm # other valid values are 'mongodb' and 'couchdb'
    firewall_name: main
    user_class: App\Entity\User
    from_email:
        address: '%env(resolve:APP_MAIL)%'
        sender_name: '%env(resolve:APP_NAME)%'
    registration:
        form:
            type: App\Form\RegistrationType
    profile:
        form:
            type: App\Form\ProfileType
