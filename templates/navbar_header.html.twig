<nav class="navbar navbar-expand-xl navbar-dark bg-primary text-white">
  <a class="navbar-brand" href="/">{{ app_name }}</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-header" aria-controls="navbarsExample06" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbar-header">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="/"></a>
      </li>
    </ul>
    <div class="form-inline my-2 my-md-0">
      <!-- Informations sur le compte utilisateur ou connexion -->
      {% if app.user %}

      {% else %}
        <a class="nav-link text-white" href="{{ path('fos_user_security_login') }}">
          <span class="glyphicon glyphicon-user" aria-hidden="true">Se connecter</span>
        </a>
      {% endif %}
    </div>
  </div>
</nav>
